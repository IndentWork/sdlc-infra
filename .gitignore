# Terraform local state and cache — never commit these
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan

# All variable values live in terraform.auto.tfvars which is committed.
# No sensitive values in tfvars — subscription_id comes from ARM_SUBSCRIPTION_ID env var.

# OS
.DS_Store
